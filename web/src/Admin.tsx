import { useEffect, useMemo, useRef, useState } from 'react';
import { ArrowLeft, ArrowUpRight, Check, Download, Flag, GitCompareArrows, Layers, MapPin, Pencil, Plus, RefreshCw, Route as RouteIcon, Save, Shield, Trash2, Undo2, Redo2, Upload, X, LockKeyhole, Building2 } from 'lucide-react';
import { TerraDraw, TerraDrawPointMode, TerraDrawLineStringMode, TerraDrawPolygonMode, TerraDrawSelectMode, TerraDrawSessionUndoRedo, type GeoJSONStoreFeatures } from 'terra-draw';
import { TerraDrawMapLibreGLAdapter } from 'terra-draw-maplibre-gl-adapter';
import type { GeoJSONSource, Map as MapInstance } from 'maplibre-gl';
import type { Feature, Geometry } from 'geojson';
import { Button } from '@/components/ui/button';
import { MapView } from './MapView';
import { api, supabase } from './supabase';
import { applyEdits, assembleSources, validateEdit, type SourceRecord } from './editor-model';
import { distance } from './geo';
import { findRoutes } from './routing';
import type { CampusData, MapChange, MapEdit, Place, Position, Release, Route, StudentReport } from './types';
interface EditorState { edits: MapEdit[]; changes: MapChange[]; reports: StudentReport[]; jobs: { id: string; kind: string; status: string; message: string; created_at: string }[]; releases: Release[] }
const empty: EditorState = { edits: [], changes: [], reports: [], jobs: [], releases: [] };
const geometryMode = (g: Geometry) => g.type === 'Point' ? 'point' : g.type === 'Polygon' ? 'polygon' : 'linestring';
export default function Admin({ data }: { data: CampusData }) {
 const [signedIn, setSignedIn] = useState(false), [authorized, setAuthorized] = useState(false), [state, setState] = useState<EditorState>(empty), [sources, setSources] = useState<SourceRecord[]>([]);
 const [tab, setTab] = useState<'edit' | 'changes' | 'reports' | 'releases'>('edit'), [error, setError] = useState(''), [message, setMessage] = useState(''), [busy, setBusy] = useState(false);
 const [current, setCurrent] = useState<MapEdit | null>(null), [dirty, setDirty] = useState(false), [confirmDelete, setConfirmDelete] = useState(false), [summary, setSummary] = useState(''), [review, setReview] = useState<MapChange | null>(null);
 const [preview, setPreview] = useState(false), [testOrigin, setTestOrigin] = useState(''), [testDest, setTestDest] = useState(''), [testRoutes, setTestRoutes] = useState<Route[]>([]);
 const draw = useRef<TerraDraw | null>(null), mapRef = useRef<MapInstance | null>(null), currentRef = useRef<MapEdit | null>(null), setting = useRef(false), modeKind = useRef<MapEdit['kind']>('place'); currentRef.current = current;
 const base = useMemo(() => assembleSources(sources, data), [sources, data]);
 const validation = useMemo(() => applyEdits(base, state.edits), [base, state.edits]);
 const visibleData = preview ? validation.data : base;
 const refresh = async () => { setError(''); try { const result = await api<EditorState>('state'); const source = await api<{features:SourceRecord[]}>('sources'); setState(result); setSources(source.features); setAuthorized(true); } catch(e) { setError((e as Error).message); } };
 useEffect(() => { if (!supabase) return; void supabase.auth.getSession().then(({data})=>{setSignedIn(!!data.session);if(data.session)void refresh();}); const {data:listener}=supabase.auth.onAuthStateChange((_event,session)=>{setSignedIn(!!session);if(!session)setAuthorized(false);}); return ()=>listener.subscription.unsubscribe(); }, []);
 useEffect(() => { if(signedIn&&!authorized)void refresh(); }, [signedIn]);
 useEffect(() => { const beforeUnload=(e:BeforeUnloadEvent)=>{if(dirty){e.preventDefault();e.returnValue='';}};window.addEventListener('beforeunload',beforeUnload);return()=>window.removeEventListener('beforeunload',beforeUnload);},[dirty]);
 const action = async (name: string, payload: unknown, success: string) => { setBusy(true);setError('');try { await api(name,payload);setMessage(success);await refresh();return true;}catch(e){setError((e as Error).message);return false;}finally{setBusy(false);} };
 const showFeature = (edit: MapEdit) => {
  if(dirty&&!window.confirm('Discard unsaved changes to this feature?'))return;
  setPreview(false); setTab('edit');setCurrent(structuredClone(edit));currentRef.current=structuredClone(edit);setDirty(false);setConfirmDelete(false);
  if(draw.current){setting.current=true;draw.current.clear();const result=draw.current.addFeatures([{type:'Feature',id:edit.id,geometry:edit.geometry,properties:{mode:geometryMode(edit.geometry)}} as GeoJSONStoreFeatures]);draw.current.setMode('select');if(result[0]?.valid)draw.current.selectFeature(edit.id);setting.current=false;}
 };
 const editPlace = (place: Place) => showFeature(state.edits.find(e=>e.id===place.id&&e.kind==='place')||{id:place.id,kind:'place',geometry:{type:'Point',coordinates:place.coordinates},properties:{name:place.name,category:place.category,aliases:place.aliases.join(', '),department:place.department||'',faculty:place.faculty||''}});
 const newFeature = (kind: MapEdit['kind']) => { if(dirty&&!window.confirm('Discard the unsaved feature?'))return;setPreview(false);setCurrent(null);currentRef.current=null;setDirty(false);modeKind.current=kind;draw.current?.clear();draw.current?.setMode(kind==='path'||kind==='barrier'?'linestring':kind==='building'?'polygon':'point');setMessage(kind==='closure'?'Click the map for a closure marker, then click a path to block it.':`Draw the ${kind} on the map. Double-click to finish a line or outline.`); };
 const changeProperty = (key:string,value:unknown) => { setCurrent(previous=>previous?{...previous,properties:{...previous.properties,[key]:value}}:null);setDirty(true); };
 const mapReady = (map:MapInstance) => {
  mapRef.current=map;
  const flags={feature:{draggable:true,coordinates:{draggable:true,midpoints:true,deletable:true}}};
  const instance=new TerraDraw({adapter:new TerraDrawMapLibreGLAdapter({map}),idStrategy:{getId:()=>crypto.randomUUID(),isValidId:(id)=>typeof id==='string'},modes:[new TerraDrawPointMode(),new TerraDrawLineStringMode(),new TerraDrawPolygonMode(),new TerraDrawSelectMode({flags:{point:flags,linestring:flags,polygon:flags}})],undoRedo:{sessionLevel:new TerraDrawSessionUndoRedo()}});
  draw.current=instance;instance.start();instance.setMode('select');
  instance.on('finish',id=>{const feature=instance.getSnapshotFeature(id);if(!feature)return;const edit:MapEdit={id:String(id),kind:modeKind.current,geometry:feature.geometry,properties:{name:'',category:'other'}};currentRef.current=edit;setCurrent(edit);setDirty(true);instance.setMode('select');instance.selectFeature(id);});
  instance.on('change',(_ids,type)=>{if(setting.current||!currentRef.current||type==='create')return;const feature=instance.getSnapshotFeature(currentRef.current.id);if(feature){setCurrent(previous=>previous?{...previous,geometry:feature.geometry}:null);setDirty(true);}});
  map.addSource('graph-nodes',{type:'geojson',data:{type:'FeatureCollection',features:visibleData.graph.nodes.map(n=>({type:'Feature',properties:{id:n.id},geometry:{type:'Point',coordinates:n.coordinates}}))}});
  map.addLayer({id:'graph-nodes',type:'circle',source:'graph-nodes',minzoom:16,paint:{'circle-radius':3,'circle-color':'#1764ed','circle-stroke-color':'white','circle-stroke-width':1}});
  map.addSource('review-diff',{type:'geojson',data:{type:'FeatureCollection',features:[]}});
  map.addLayer({id:'review-fill',type:'fill',source:'review-diff',filter:['==',['geometry-type'],'Polygon'],paint:{'fill-color':['get','color'],'fill-opacity':.25}});
  map.addLayer({id:'review-line',type:'line',source:'review-diff',filter:['!=',['geometry-type'],'Point'],paint:{'line-color':['get','color'],'line-width':4}});
  map.addLayer({id:'review-point',type:'circle',source:'review-diff',filter:['==',['geometry-type'],'Point'],paint:{'circle-color':['get','color'],'circle-radius':8,'circle-opacity':.7}});
  map.on('click','roads',event=>{
   const id=event.features?.[0]?.properties?.id;if(!id||preview)return;
   if(currentRef.current&&['closure','barrier'].includes(currentRef.current.kind)) {const edgeIds=visibleData.graph.edges.filter(e=>e.sourceId===id).map(e=>e.id);changeProperty('edgeIds',edgeIds);setMessage(`${edgeIds.length} directed segments selected. Both walking directions will be blocked.`);return;}
   if(instance.getMode()!=='select')return;
   const source=visibleData.map.features.find(f=>f.properties?.id===id);if(source)showFeature(state.edits.find(e=>e.id===id)||{id:String(id),kind:'path',geometry:source.geometry,properties:{name:source.properties?.name||'Campus path',access:'yes'}});
  });
  map.on('click','buildings',event=>{if(instance.getMode()!=='select'||preview||currentRef.current?.kind==='closure')return;const id=event.features?.[0]?.properties?.id;const source=visibleData.map.features.find(f=>f.properties?.id===id);if(source)showFeature(state.edits.find(e=>e.id===id&&e.kind==='building')||{id:String(id),kind:'building',geometry:source.geometry,properties:{name:source.properties?.name||'Building',height:source.properties?.height||0,heightEstimated:source.properties?.heightEstimated||false}});});
  setMessage('Select a place, building, or path to edit it. Blue dots are explicit routing nodes.');
 };
 useEffect(()=>{const map=mapRef.current;if(!map?.getSource('review-diff'))return;const features:Feature[]=[];for(const [side,color] of [['before','#d45555'],['after','#26a07c']]){const record=(review as any)?.[side];const payload=record?.payload;const geometry=payload?.geometry||(payload?.coordinates?{type:'Point',coordinates:payload.coordinates}:null);if(geometry)features.push({type:'Feature',properties:{color},geometry});}(map.getSource('review-diff') as GeoJSONSource).setData({type:'FeatureCollection',features});if(features[0]?.geometry.type==='Point')map.flyTo({center:features[0].geometry.coordinates as Position,zoom:18});},[review]);
 const save = async () => { if(!current)return;const errors=validateEdit(current);if(errors.length){setError(errors.join(' '));return;}const result=applyEdits(base,[...state.edits.filter(e=>e.id!==current.id),current]);if(result.errors.length){setError(result.errors.join(' '));return;}if(await action('save-edit',current,'Draft saved. It is not published yet.'))setDirty(false); };
 const exportData=async()=>{try{const content=await api('export');const url=URL.createObjectURL(new Blob([JSON.stringify(content,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`turnright-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url);}catch(e){setError((e as Error).message);}};
 if(!authorized)return <main className="loading-screen"><a className="brandmark" href="/"><ArrowUpRight/></a><LockKeyhole size={28}/><h1>Campus map editor</h1><p>{!supabase?'Connect your Supabase project to enable the protected editor. The public campus map works without it.':signedIn?'Checking administrator access…':'Sign in with the GitHub account allowlisted for TurnRight.'}</p>{supabase&&!signedIn&&<Button className="primary-action" style={{maxWidth:280}} onClick={()=>supabase!.auth.signInWithOAuth({provider:'github',options:{redirectTo:location.origin+'/admin'}})}>Sign in with GitHub</Button>}{error&&<p className="form-error">{error}</p>}{signedIn&&<Button variant="outline" onClick={()=>supabase!.auth.signOut()}>Sign out</Button>}<a className="text-button" href="/">← Back to campus map</a></main>;
 return <main className="admin-shell"><header className="admin-header"><a href="/" className="admin-brand"><span className="brandmark"><ArrowUpRight/></span><strong>TurnRight<span> / Map editor</span></strong></a><div className="admin-header-actions"><span className="admin-private"><Shield size={14}/> Private workspace</span><Button variant="outline" onClick={refresh} disabled={busy}><RefreshCw/> Refresh</Button><Button variant="outline" onClick={exportData}><Download/> Export backup</Button><Button variant="ghost" onClick={()=>supabase!.auth.signOut()}>Sign out</Button></div></header><div className="admin-body"><aside className="admin-panel"><nav className="admin-tabs">{(['edit','changes','reports','releases'] as const).map(t=><button className={tab===t?'active':''} key={t} onClick={()=>setTab(t)}>{t.charAt(0).toUpperCase()+t.slice(1)}{t==='changes'&&state.changes.length>0&&<small>{state.changes.length}</small>}{t==='reports'&&state.reports.length>0&&<small>{state.reports.length}</small>}</button>)}</nav><div className="admin-scroll">
 {tab==='edit'&&<><h2>Shape the campus map</h2><p className="small-note">Changes stay in drafts until a reviewed release is published.</p><div className="draw-tools">{(['place','path','building','entrance','barrier','closure'] as const).map(kind=><Button key={kind} variant="outline" onClick={()=>newFeature(kind)}><Plus size={14}/>{kind}</Button>)}</div><label className="field-label">Edit a place<select value="" onChange={e=>{const p=base.places.find(p=>p.id===e.target.value);if(p)editPlace(p);}}><option value="">Choose a campus place…</option>{base.places.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label>{current&&<div className="edit-form"><div className="edit-kind"><span>{current.kind}</span><div><Button aria-label="Undo geometry" variant="ghost" size="icon" onClick={()=>draw.current?.undo()}><Undo2/></Button><Button aria-label="Redo geometry" variant="ghost" size="icon" onClick={()=>draw.current?.redo()}><Redo2/></Button></div></div><label className="field-label">Name / description<input value={String(current.properties.name||'')} onChange={e=>changeProperty('name',e.target.value)}/></label>{current.kind==='place'&&<><label className="field-label">Category<select value={String(current.properties.category||'other')} onChange={e=>changeProperty('category',e.target.value)}>{['academic','library','food','services','worship','residence','sports','gate','other'].map(c=><option key={c}>{c}</option>)}</select></label>{['aliases','department','faculty'].map(key=><label className="field-label" key={key}>{key}<input value={String(current.properties[key]||'')} onChange={e=>changeProperty(key,e.target.value)}/></label>)}</>}{current.kind==='building'&&<><label className="field-label">Height (metres)<input type="number" min={0} max={150} value={Number(current.properties.height)||0} onChange={e=>changeProperty('height',Number(e.target.value))}/></label><label className="checkbox-label"><input type="checkbox" checked={!!current.properties.heightEstimated} onChange={e=>changeProperty('heightEstimated',e.target.checked)}/> Height is approximate</label></>}{current.kind==='path'&&<><label className="field-label">Walking access<select value={String(current.properties.access||'yes')} onChange={e=>changeProperty('access',e.target.value)}><option value="yes">Public walking permitted</option><option value="private">Private / restricted</option><option value="no">No walking access</option></select></label>{['connectStart','connectEnd'].map(key=><label className="field-label" key={key}>{key==='connectStart'?'Connect first endpoint':'Connect last endpoint'}<select value={String(current.properties[key]||'')} onChange={e=>changeProperty(key,e.target.value)}><option value="">New independent node</option>{validation.data.graph.nodes.map(n=><option key={n.id} value={n.id}>{n.coordinates[1].toFixed(5)}, {n.coordinates[0].toFixed(5)} · {n.id.slice(-18)}</option>)}</select></label>)}<p className="small-note">Choose explicit endpoint connections within 5 m. Crossing lines alone do not create a junction.</p><label className="checkbox-label"><input type="checkbox" checked={!!current.properties.steps} onChange={e=>changeProperty('steps',e.target.checked)}/> Includes steps</label></>}{current.kind==='entrance'&&<><label className="field-label">Building / place<select value={String(current.properties.placeId||'')} onChange={e=>changeProperty('placeId',e.target.value)}><option value="">Choose place</option>{base.places.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label className="field-label">Connect to path node<select value={String(current.properties.connectTo||'')} onChange={e=>changeProperty('connectTo',e.target.value)}><option value="">Choose existing node within 5 m</option>{validation.data.graph.nodes.map(n=><option value={n.id} key={n.id}>{n.coordinates[1].toFixed(5)}, {n.coordinates[0].toFixed(5)}</option>)}</select></label></>}{['closure','barrier'].includes(current.kind)&&<><p className="notice">Click a mapped path to select the segments this {current.kind} blocks.</p><p className="small-note">{Array.isArray(current.properties.edgeIds)?current.properties.edgeIds.length:0} directed segments selected.</p><label className="field-label">Expected reopening<input type="datetime-local" value={String(current.properties.expectedReopening||'').slice(0,16)} onChange={e=>changeProperty('expectedReopening',e.target.value)}/></label><p className="small-note">Paths remain blocked until you explicitly confirm reopening.</p><label className="checkbox-label"><input type="checkbox" checked={!!current.properties.reopenedAt} onChange={e=>changeProperty('reopenedAt',e.target.checked?new Date().toISOString():undefined)}/> I have confirmed this path is open</label></>}
 <div className="button-row"><Button onClick={save} disabled={busy||!dirty}><Save/> Save draft</Button><Button variant="destructive" onClick={()=>setConfirmDelete(true)}><Trash2/> Delete</Button></div>{confirmDelete&&<div className="notice">Delete this feature in the next release?<div className="button-row"><Button variant="outline" onClick={()=>setConfirmDelete(false)}>Cancel</Button><Button variant="destructive" onClick={async()=>{if(await action('save-edit',{...current,deleted:true},'Deletion saved as a draft.')){setCurrent(null);setDirty(false);draw.current?.clear();}}}>Confirm deletion</Button></div></div>}</div>}
 <h3 className="subheading">Saved corrections ({state.edits.length})</h3>{state.edits.map(edit=><button key={edit.id} className="admin-list-row" onClick={()=>showFeature(edit)}><Pencil size={16}/><span>{String(edit.properties.name)}<small>{edit.kind}{edit.deleted?' · marked for deletion':''}</small></span></button>)}<h3 className="subheading">Route test</h3><p className="small-note">Test the current saved drafts before publishing.</p>{[['From',testOrigin,setTestOrigin],['To',testDest,setTestDest]].map(([label,value,setter])=><label className="field-label" key={String(label)}>{String(label)}<select value={String(value)} onChange={e=>(setter as (s:string)=>void)(e.target.value)}><option value="">Choose connected place</option>{validation.data.places.filter(p=>p.graphNode).map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label>)}<Button variant="outline" disabled={!testOrigin||!testDest} onClick={()=>{try{const from=validation.data.places.find(p=>p.id===testOrigin)!,to=validation.data.places.find(p=>p.id===testDest)!;setTestRoutes(findRoutes(validation.data,from.graphNode!,to.graphNode!));setMessage('Draft walking route shown on the map.');}catch(e){setError((e as Error).message);}}}><RouteIcon/> Test walking route</Button></>}
 {tab==='changes'&&<><h2>Source review</h2><p className="small-note">Daily checks propose changes. Your campus corrections always take precedence.</p><Button variant="outline" disabled={busy} onClick={()=>action('check-sources',{},'Source check queued. Refresh to see its result.')}><RefreshCw/> Check now</Button>{state.jobs.slice(0,3).map(job=><div className={`job-status ${job.status}`} key={job.id}><strong>{job.status}</strong><p>{job.message||'Import in progress'}</p><small>{new Date(job.created_at).toLocaleString()}</small></div>)}{state.changes.length===0&&<div className="empty-state"><GitCompareArrows/><h3>No pending changes</h3><p>New source differences will appear here.</p></div>}{state.changes.map(change=><div className={`change-card ${review?.id===change.id?'selected':''}`} key={change.id}><button onClick={()=>setReview(change)}><span className="change-kind">{change.kind}</span><strong>{change.summary}</strong></button><p className="small-note">{change.source_id}</p><div className="button-row"><Button variant="outline" size="sm" disabled={busy} onClick={()=>action('review-change',{id:change.id,accept:false},'Change rejected; source baseline retained.')}><X/> Reject</Button><Button size="sm" disabled={busy} onClick={()=>action('review-change',{id:change.id,accept:true},'Change accepted into the source baseline. Publish a release to update the public map.')}><Check/> Accept</Button></div>{review?.id===change.id&&<details><summary>Before / after values</summary><pre>{JSON.stringify({before:change.before,after:change.after},null,2)}</pre></details>}</div>)}</>}
 {tab==='reports'&&<><h2>Student reports</h2><p className="small-note">Reports are private. Review the location before changing the map.</p>{!state.reports.length&&<div className="empty-state"><Flag/><h3>No pending reports</h3></div>}{state.reports.map(report=><div className="change-card" key={report.id}><strong>{report.category.replaceAll('-',' ')}</strong><p>{report.description}</p><button className="text-button" onClick={()=>mapRef.current?.flyTo({center:report.coordinates,zoom:19})}><MapPin size={15}/> Show location</button><div className="button-row"><Button variant="outline" disabled={busy} onClick={()=>action('resolve-report',{id:report.id,status:'dismissed'},'Report dismissed.')}>Dismiss</Button><Button disabled={busy} onClick={()=>action('resolve-report',{id:report.id,status:'resolved'},'Report marked resolved.')}>Mark resolved</Button></div></div>)}</>}
 {tab==='releases'&&<><h2>Review, then publish</h2><div className="validation-card"><strong>{validation.errors.length?'Validation needs attention':'Draft validation passed'}</strong><p>{validation.data.places.length} places · {validation.data.graph.edges.length} path segments</p>{validation.errors.map(e=><p className="form-error" key={e}>{e}</p>)}{validation.warnings.map(w=><p className="small-note" key={w}>{w}</p>)}</div><p className="notice">{validation.data.coverage.disconnected.length} places still lack a mapped approach. Source-derived routes have not been field-verified.</p><Button variant="outline" onClick={()=>{setPreview(!preview);setCurrent(null);setDirty(false);}}><Layers/>{preview?'Show published base':'Preview saved drafts'}</Button><label className="field-label">What changed?<textarea value={summary} maxLength={500} placeholder="Describe the corrections in this release…" onChange={e=>setSummary(e.target.value)}/></label><Button disabled={busy||dirty||validation.errors.length>0||summary.trim().length<5} onClick={()=>action('prepare-release',{summary},'Immutable release queued. Review its deployment preview before publishing.')}><Upload/> Build review preview</Button>{state.releases.map(release=><div className="change-card" key={release.id}><span className="change-kind">{release.status}</span><h3>{release.summary}</h3><small>{new Date(release.created_at).toLocaleString()}</small>{release.error&&<p className="form-error">{release.error}</p>}{release.preview_url&&<a className="source-link" href={release.preview_url} target="_blank" rel="noreferrer">Open exact release preview ↗</a>}{release.status==='preview'&&<Button disabled={busy} onClick={()=>{if(window.confirm('Publish this reviewed preview to the public TurnRight site?'))void action('publish-release',{id:release.id},'Publication requested. Refresh to verify the result.');}}>Publish this preview</Button>}{release.status==='published'&&<Button variant="outline" disabled={busy} onClick={()=>{if(window.confirm('Restore this previously published release?'))void action('rollback',{id:release.id},'Restore requested. Refresh to verify the result.');}}>Restore this release</Button>}</div>)}</>}
 </div></aside><section className="admin-map"><MapView data={visibleData} routes={testRoutes} onSelect={editPlace} onReady={mapReady}/><div className="editor-map-status"><span className="status-dot"/>{preview?'Saved draft preview':'Source map · click to edit'}{dirty&&<strong>Unsaved feature</strong>}</div>{message&&<div className="editor-message" role="status">{message}<button aria-label="Dismiss message" onClick={()=>setMessage('')}><X size={16}/></button></div>}</section></div>{error&&<div className="toast" role="alert"><span>{error}</span><button aria-label="Dismiss error" onClick={()=>setError('')}><X size={18}/></button></div>}</main>;
}
